%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:solves, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:solves, :new)

.tabs-content
  -form_for :solve, url(:solves, :create), :class => 'form-horizontal' do |f|
    =partial 'solves/form', :locals => { :f => f }
